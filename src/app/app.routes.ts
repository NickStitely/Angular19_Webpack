import { Routes } from '@angular/router';
import { UserComponent } from './user-module/user.component';

export const routes: Routes = [
    {
        path: "user",
        component: UserComponent
    }
];
